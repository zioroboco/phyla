{
  "private": true,
  "workspaces": [
    "begat"
  ],
  "jest": {
    "projects": [
      "begat"
    ]
  },
  "prettier": {
    "arrowParens": "avoid",
    "semi": false
  },
  "scripts": {
    "core": "yarn workspace begat",
    "build": "yarn workspaces foreach run build",
    "clean": "yarn workspaces foreach run clean",
    "format": "yarn dlx prettier --write '**/*[!.d].{json,mjs,ts,yaml}'",
    "test": "jest"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.2",
    "@types/jest": "*",
    "@types/node": "*",
    "jest": "^27.2.2",
    "ts-expect": "^1.3.0",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
