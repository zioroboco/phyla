{
  "private": true,
  "workspaces": [
    "begat"
  ],
  "jest": {
    "projects": [
      "begat"
    ]
  },
  "scripts": {
    "build": "yarn -s workspaces run build",
    "clean": "yarn -s workspaces run clean",
    "format": "npx prettier --write '**/*.{json,mjs,ts,yaml}'",
    "test": "jest"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.2",
    "@types/jest": "*",
    "@types/node": "*",
    "jest": "^27.2.2",
    "ts-expect": "^1.3.0",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
