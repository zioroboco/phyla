{
  "packageManager": "pnpm@7.6.0",
  "private": true,
  "scripts": {
    "just-extras": "just-extras --omit=install",
    "just-check": "pnpm -s just-extras check",
    "just-sync": "pnpm -s just-extras sync",
    "build": "just build",
    "clean": "just clean",
    "format": "just format",
    "release": "just release",
    "test": "just test",
    "version": "just version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.23.0",
    "@phyla/core": "workspace:*",
    "@zioroboco/changelog-github": "^0.1.1",
    "just-extras": "^0.3.0",
    "just-install": "^1.0.8",
    "tsm": "^2.2.1",
    "typescript": "^4.7.4"
  }
}
